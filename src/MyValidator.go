package src

import (
	"github.com/go-playground/validator/v10"
	"reflect"
)

func TopicUrl(level *validator.Validate, topicStruct reflect.Value, currentStruct reflect.Value,
	field reflect.Value, fieldType reflect.Type, fieldKind reflect.Kind, param string) bool {
	return false
}
